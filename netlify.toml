[build]
  command = "npm run build"
  publish = "dist/public"

[dev]
  command = "cross-env NODE_ENV=development tsx server/index.ts"
  

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true
